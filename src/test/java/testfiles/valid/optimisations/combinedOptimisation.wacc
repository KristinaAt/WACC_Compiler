# constant propagation, constant folding, condition simplification and dead constant elimination test

# Output:
# 4

# Program:

begin
 int a = 30;
 int b = 9 - (a / 5);
 int c = b * 4;
 if c > 10
 then
      c = c - 10
 else
   println c * (60 / a)
 fi
end
 
